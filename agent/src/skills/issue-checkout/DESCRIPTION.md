Create and switch to a working branch derived from a GitHub Issue. Fetches the Issue via the `view-issue` sub-skill, maps its label to the CONTRIBUTING.md prefix, generates a kebab-case English `summary` from the title, validates the assembled `{prefix}/{issue-number}/{summary}` via the `validate-branch-name` sub-skill, ensures no local/remote collision, then runs `git checkout -b`.
